// Define the type for a sidebar link
export type SidebarLink = {
  route: string;
  label: string;
};

// Define the type for a slide in a slider
export type Slide = {
  url: string;
  tag: string;
  desc: string;
};
